var utils = {
	isMobile : function() {
		var filter = "win16|win32|win64|mac";
		if( navigator.platform  ){
			if( filter.indexOf(navigator.platform.toLowerCase())<0 ){
			    console.log("util.isMobile : 모바일 기기에서 접속");
			    return true;
			}else{
				console.log("util.isMobile : PC에서 접속");
			    return false;
			}
		}
	},
	getPhoto : function() {
		
	},
	getCamera : function() {
		
	},
	getMovie : function() {
		
	}
};